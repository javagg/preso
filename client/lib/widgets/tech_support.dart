import 'package:flutter/material.dart';
import 'package:get/get.dart';

class TechSupport extends StatelessWidget {
  const TechSupport({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('app.techSupport'.tr);
  }
 
}