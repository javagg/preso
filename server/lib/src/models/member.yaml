class: Member
table: member
fields:
  name: String
  gender: String
  age: int
  mobile: String
  suspended: bool, default=false
  headshot: String
  face: String
  appointments: List<Appointment>?, relation(name=member_appointment)
  membership: List<Membership>?, relation(name=member_membership)
